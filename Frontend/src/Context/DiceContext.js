// import React, { useContext, useContext } from "react";
import { buildRollManagerFresh } from "../Services/roll-manager/creation";
import { DEFAULT_ROLL_MANAGER_SETTINGS } from "../Global";

// const DiceContext = React.createContext({
//   data: buildRollManagerFresh(DEFAULT_ROLL_MANAGER_SETTINGS),
//   setData: (-objWitDataRollManager) => {},
// });
//
// export const DiceContextWrapper = ({ children }) => {
//   const [data, setDate] = useState(
//     buildRollManagerFresh(DEFAULT_ROLL_MANAGER_SETTINGS)
//   );
//   const [] = useState();
//   const value = useMemo(() => ({
//     data,
//     setData: ({}) => {},
//   }));
// };
